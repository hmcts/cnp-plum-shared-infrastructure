#!groovy
//noinspection GroovyUnusedAssignment
@Library("Infrastructure@dtspo-22016-not-on-pr") _

def product = "plum"
def branchesToSync = ['ithc', 'perftest', 'demo']

withInfraPipeline(product) {
  syncBranchesWithMaster(branchesToSync)
}
